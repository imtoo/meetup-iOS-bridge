# Development
### Start packager
```bash
yarn install
yarn start
```
and then build app from XCode

### Troubleshooting
In case of weird compile errors with RCT headers run
```bash
cd ios
pod install
```
